{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-acquisitions",
      "version": "^2.1.0-SNAPSHOT",
      "preRelease": "only"
    }
  ],
  "modules": [
    {
      "name": "mod-ebsconet",
        "version": "latest",
        "preRelease": "only"
    }
  ]
}
